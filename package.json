{
  "scripts": {
    "dev": "parcel ./public/index.html --port 8080 && nodemon",
    "fresh-build": "rm -rf output && spago build -d"
  },
  "dependencies": {
    "normalize.css": "^8.0.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.2",
    "parcel-bundler": "^1.12.4",
    "purty": "^6.0.0",
    "sass": "^1.26.2",
    "spago": "^0.14.0"
  }
}
